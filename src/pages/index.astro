---
import HomePage from '@/components/HomePage.astro';
import { copy } from '@/data/content';
import BaseLayout from '@/layouts/BaseLayout.astro';

const c = copy.en;
---

<BaseLayout lang="en" title={c.seo.title} description={c.seo.description} path={c.path} alternatePath={c.alternatePath} profilePage>
  <HomePage locale="en" />
</BaseLayout>
